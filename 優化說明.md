# 網站優化說明

三個檔案都已優化完成，功能與外觀完全不變，可以直接取代原檔上傳。以下是改了什麼、為什麼改。

## index.html（前台）

**清掉重複程式碼**：`closeNav` / `toggleNav` 原本定義了三次，nav 連結的點擊事件也綁了兩次，另外有一個從未被呼叫的舊版 `initPano360()`。已全部清除，只保留最終版本（含手機選單鎖定捲動的那版）。

**防範資料注入（XSS）**：原本卡片標題、地點、標籤、描述等欄位是直接串進 HTML。因為 Apps Script 端目前沒有驗證寫入者，理論上任何人都能把惡意 HTML 塞進 Google Sheet，再透過前台執行。現在所有資料欄位都會先經過 `esc()` 跳脫處理，搜尋關鍵字標亮（highlight）功能照常運作。

**搜尋加入 debounce**：打字時不再每按一個鍵就重建整個卡片列表，改為停止輸入 180ms 後才重繪，在資料多的時候打字會明顯更順。清除按鈕仍是即時反應。

**搜尋更準確**：搜尋描述欄位時改用純文字內容比對，不會再誤中分段描述底層 JSON 格式裡的 `title`、`text` 等符號。

**減少畫面閃動**：開站時會先顯示快取資料、再抓最新資料。現在若最新資料跟快取一模一樣，就跳過重繪，不會再閃一下。

## admin.html（後台）

**密碼不再以明文儲存**：原本帳密以明文存在 localStorage，「忘記密碼」信件還會直接夾帶密碼。現在改為 SHA-256 雜湊儲存與比對；舊版已存的明文帳號會在下次登入成功時自動升級成雜湊；重置信件不再包含密碼明文。

**新增寫入保護的掛鉤（建議啟用）**：檔案開頭多了一個 `API_TOKEN` 常數，儲存資料時會一併送到 Apps Script。目前留空、行為與現在完全相同；啟用方式見下方「重要：後端安全」。

**GitHub Token 欄位加上提示**：建議改用 Fine-grained token、只授權單一 Repo 並設定到期日（360viewer.html 同步加上）。

## 360viewer.html（360 環景工具）

這支寫得很好（延遲載入 Three.js、切片載入、貼圖記憶體釋放都有做），只做了小修正：loading 逾時的註解與實際數值不一致（註解寫 8 秒、實際 4 秒）已統一，以及上述 token 提示。

---

## 重要：後端安全（強烈建議處理）

目前最大的風險不在這三個檔案，而是 **Apps Script 的 POST 端點沒有任何驗證**——後台登入只是前端檢查，任何看過原始碼的人都可以直接對 API 發 POST，覆寫你 Google Sheet 裡的所有資料。

修補方式（約 5 分鐘）：

1. 想一組長一點的密語，例如 `sl-2026-x7Kp9mQ2`。
2. 在 `admin.html` 開頭把 `var API_TOKEN = '';` 改成 `var API_TOKEN = 'sl-2026-x7Kp9mQ2';`
3. 到 Apps Script 編輯器，在 `doPost` 最前面加上驗證：

```javascript
function doPost(e) {
  var body = JSON.parse(e.postData.contents);

  // ── 寫入保護：密語不符就拒絕 ──
  var TOKEN = 'sl-2026-x7Kp9mQ2';  // 與 admin.html 的 API_TOKEN 相同
  if (body.token !== TOKEN) {
    return ContentService
      .createTextOutput(JSON.stringify({ error: 'unauthorized' }))
      .setMimeType(ContentService.MimeType.JSON);
  }

  // ……以下接你原本的儲存邏輯（body.sheet、body.data）……
}
```

4. 重新部署 Apps Script（部署 → 管理部署 → 編輯 → 新版本）。

注意：因為 admin.html 是公開檔案，密語仍會出現在原始碼中，這擋的是「隨手亂改的人」。如果想要更高強度，可以不把密語寫進檔案，改成後台登入後請你手動輸入一次、存在瀏覽器 localStorage——需要的話我可以再幫你加。

## 其他建議（不急）

- **Cloudinary 上傳**：目前用的是 unsigned preset，任何人都能往你的 Cloudinary 空間上傳圖片。建議到 Cloudinary 後台把該 preset 限制資料夾、檔案大小與格式，並開啟 moderation。
- **GitHub Pages 快取**：`data.json` 若有使用，記得在 admin 儲存後也同步更新，否則第一次載入會先看到舊資料。
